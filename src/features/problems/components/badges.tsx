import { Badge } from "@/components/ui/badge";

export const DraftBadge = () => <Badge variant="secondary">Draft</Badge>;
export const RestrictedBadge = () => <Badge variant="destructive">Restricted</Badge>;
